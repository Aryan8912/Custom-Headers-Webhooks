# ── Core Testing ───────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0

# ── Async test client (hit FastAPI endpoints in tests) ─
httpx==0.27.2

# ── HTTP mocking (mock downstream CRM / forward calls) ─
respx==0.21.1

# ── In-process Redis mock (no real Redis needed) ───────
fakeredis==2.23.3

# ── Fake data for fixtures ─────────────────────────────
faker==26.0.0

# ── Env var injection for pytest ───────────────────────
pytest-env==1.1.3

# ── Async SQLite for in-memory DB in tests ─────────────
aiosqlite==0.20.0